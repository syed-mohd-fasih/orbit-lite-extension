import type { RegisteredCourse } from "../../types/course";

//⚠️ STUB — the page is blank outside the registration window.
export function parseRegisteredCourses(): RegisteredCourse[] | null {
	console.warn("[Orbit Lite] registeredCourses parser not implemented yet — page blank out of season");
	return null;
}
